[project]
name = "parser"
version = "0.1.0"
description = "Парсер русскоязычных новостей с сохранением датасета в XML"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "requests>=2.33.1",
]
